create database inventario;

use inventario;

create table productos(codigo varchar(5),
					nombre varchar(30),
					descripcion varchar (100),
					precio_compra money,
					precio_venta money,
					existencia int,
					constraint primary key

create table clientes();

create table apartados();

create table ventas();
